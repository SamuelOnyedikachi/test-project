import 'package:flutter/material.dart';

class AppRadius {
  static BorderRadius small = BorderRadius.circular(12);
  static BorderRadius medium = BorderRadius.circular(18);
  static BorderRadius large = BorderRadius.circular(30);
}
